# 🤖 GameSwap Spain Bot

Telegram бот для обмена физическими играми между геймерами в Испании.

## 📋 Функционал

### Для пользователей:
- ✅ Регистрация с указанием города
- ✅ Добавление игр в каталог (с фото)
- ✅ Поиск игр по названию
- ✅ Просмотр всего каталога
- ✅ Личный профиль с рейтингом
- ✅ Система подтверждения обменов
- ✅ Автоматическая публикация в группу

### Для админа:
- ✅ Статистика пользователей и обменов
- ✅ Модерация (бан пользователей)

## 🚀 Установка и запуск

### Вариант 1: Локально (для теста)

1. **Установи Python 3.9+** (если ещё нет)
   
2. **Скачай все файлы** в одну папку

3. **Установи зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Создай файл .env** (скопируй из .env.example):
   ```bash
   cp .env.example .env
   ```

5. **Отредактируй .env:**
   ```
   BOT_TOKEN=твой_токен_от_BotFather
   GROUP_CHAT_ID=-100123456789  (получишь позже)
   ADMIN_ID=твой_telegram_id
   ```

6. **Запусти бота:**
   ```bash
   python bot.py
   ```

7. **Бот работает!** Найди его в Telegram по username

### Вариант 2: На Railway (бесплатный хостинг)

1. **Зарегистрируйся на Railway.app**
   - Зайди на https://railway.app
   - Зарегистрируйся через GitHub

2. **Создай новый проект:**
   - New Project → Deploy from GitHub repo
   - Подключи свой GitHub аккаунт
   - Выбери репозиторий с кодом бота

3. **Настрой переменные окружения:**
   - В проекте Railway → Variables
   - Добавь:
     - `BOT_TOKEN` = твой токен
     - `GROUP_CHAT_ID` = ID группы
     - `ADMIN_ID` = твой ID

4. **Деплой:**
   - Railway автоматически запустит бота
   - Логи можно смотреть в интерфейсе

## 🔧 Получение GROUP_CHAT_ID

1. Добавь бота в свою группу как администратора
2. Напиши любое сообщение в группе
3. Открой: `https://api.telegram.org/bot<BOT_TOKEN>/getUpdates`
4. Найди "chat":{"id":-100XXXXXXXXX
5. Скопируй это число (с минусом) в .env

## 📝 Команды бота

### Пользовательские:
- `/start` - регистрация
- `/add` - добавить игру
- `/mygames` - мои игры
- `/search` - найти игру
- `/catalog` - весь каталог
- `/profile` - мой профиль
- `/help` - помощь

### Админские:
- `/stats` - статистика

## 🗄️ База данных

Бот использует SQLite (файл `gameswap.db`).

Таблицы:
- `users` - пользователи
- `games` - игры
- `swaps` - обмены

## 📦 Файлы проекта

```
gameswap_bot/
├── bot.py              # Основной код бота
├── database.py         # Работа с базой данных
├── requirements.txt    # Зависимости Python
├── .env.example        # Шаблон настроек
├── .env               # Твои настройки (не коммитить!)
├── README.md          # Этот файл
└── gameswap.db        # База данных (создаётся автоматически)
```

## 🐛 Решение проблем

**Бот не запускается:**
- Проверь что установлены все зависимости: `pip install -r requirements.txt`
- Проверь что BOT_TOKEN правильный в .env
- Проверь что файл .env в той же папке что и bot.py

**Бот не публикует в группу:**
- Проверь что бот добавлен в группу как админ
- Проверь что GROUP_CHAT_ID правильный (с минусом!)
- Дай боту права на отправку сообщений

**База данных не создаётся:**
- Проверь права на запись в папке
- База создаётся автоматически при первом запуске

## 💡 Следующие шаги

После запуска бота:
1. Протестируй все команды
2. Пригласи первых пользователей
3. Соберите обратную связь
4. Добавь новые функции по необходимости

## 🆘 Поддержка

Если что-то не работает - пиши @твой_username
